import type { Dispatch, FormEvent, SetStateAction } from 'react'
export type NoticeFormState={title:string;body:string;severity:string;audienceRole:string;eventStart:string;eventEnd:string}
type Props={form:NoticeFormState;setForm:Dispatch<SetStateAction<NoticeFormState>>;onSubmit:(event:FormEvent)=>void}
const datePart=(value:string)=>value.split('T')[0]||''
const timePart=(value:string)=>value.split('T')[1]?.slice(0,5)||''
const combine=(date:string,time:string,fallback:string)=>date?`${date}T${time||fallback}`:''
export default function NoticeComposer({form,setForm,onSubmit}:Props){return <form className="notice-composer reliable-notice-composer" onSubmit={onSubmit}><h3>Submit a public notice</h3><label>Title<input value={form.title} onChange={event=>setForm({...form,title:event.target.value})} minLength={3} maxLength={200} required/></label><label>Information<textarea value={form.body} onChange={event=>setForm({...form,body:event.target.value})} minLength={3} maxLength={4000} required/></label><div className="form-pair"><label>Importance<select value={form.severity} onChange={event=>setForm({...form,severity:event.target.value})}><option>Information</option><option>Important</option><option>Urgent</option></select></label><label>Audience<select value={form.audienceRole} onChange={event=>setForm({...form,audienceRole:event.target.value})}><option value="">All members</option>{['Administrator','Research Manager','Research Officer','Reviewer'].map(role=><option key={role}>{role}</option>)}</select></label></div><fieldset className="notice-date-fields"><legend>Calendar event (optional)</legend><div className="form-pair"><label>Start date<input type="date" value={datePart(form.eventStart)} onInput={event=>{const date=event.currentTarget.value;setForm(current=>({...current,eventStart:combine(date,timePart(current.eventStart),'09:00'),eventEnd:current.eventEnd||combine(date,'','17:00')}))}}/></label><label>Start time<input type="time" value={timePart(form.eventStart)} disabled={!form.eventStart} onInput={event=>setForm(current=>({...current,eventStart:combine(datePart(current.eventStart),event.currentTarget.value,'09:00')}))}/></label></div><div className="form-pair"><label>End date<input type="date" value={datePart(form.eventEnd)} min={datePart(form.eventStart)||undefined} disabled={!form.eventStart} onInput={event=>setForm(current=>({...current,eventEnd:combine(event.currentTarget.value,timePart(current.eventEnd),'17:00')}))}/></label><label>End time<input type="time" value={timePart(form.eventEnd)} disabled={!form.eventEnd} onInput={event=>setForm(current=>({...current,eventEnd:combine(datePart(current.eventEnd),event.currentTarget.value,'17:00')}))}/></label></div><small>Choose the start date first. Times are saved using your local timezone.</small></fieldset><button className="settings-save">Submit for approval</button></form>}
